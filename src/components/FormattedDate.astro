---
interface Props {
	date: Date;
	withEmoji?: boolean;
}

const { date, withEmoji = false } = Astro.props;
---

<time datetime={date.toISOString()}>{withEmoji && "📆 "}
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
